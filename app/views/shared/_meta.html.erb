<% content_for :meta do %>
    <% meta = {:description => (obj.metatag_description if obj) || 'forex', :keywords => (obj.metatag_keywords if obj) || 'forex'} %>
    <%= meta_tags(meta) %>
<% end %>